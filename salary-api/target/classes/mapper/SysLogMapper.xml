<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuhao.payroll.mapper.SysLogMapper">
    
    <resultMap id="sysLogMap" type="com.xuhao.payroll.model.SysLog">
        <id column="log_id" property="logId"/>
        <result column="username" property="username"/>
        <result column="operation" property="operation"/>
        <result column="method" property="method"/>
        <result column="params" property="params"/>
        <result column="ip" property="ip"/>
        <result column="create_time" property="createTime"/>
        <result column="execution_time" property="executionTime"/>
        <result column="status" property="status"/>
        <result column="error_message" property="errorMessage"/>
    </resultMap>

    <insert id="insert" parameterType="com.xuhao.payroll.model.SysLog">
        INSERT INTO sys_log (
            username, operation, method, params, ip,
            create_time, execution_time, status, error_message
        ) VALUES (
            #{username}, #{operation}, #{method}, #{params}, #{ip},
            #{createTime}, #{executionTime}, #{status}, #{errorMessage}
        )
    </insert>

    <delete id="deleteBatch">
        DELETE FROM sys_log WHERE log_id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="queryList" resultMap="sysLogMap">
        SELECT * FROM sys_log
        <where>
            <if test="username != null and username != ''">
                AND username LIKE '%${username}%'
            </if>
            <if test="operation != null and operation != ''">
                AND operation LIKE '%${operation}%'
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
</mapper> 